<CollapsablePanel as |panel|>
  <panel.header>
    <h3>Timeseries</h3>
  </panel.header>

  <panel.content>
    <C3Chart 
      @data={{this.data}} 
      @axis={{this.axis}} 
      @title={{this.title}} 
      @padding={{this.padding}}
    />
  </panel.content>
</CollapsablePanel>

<div class="container mt-2">
  <button class="btn btn-sm btn-primary" {{on "click" (action this.animateChart)}}>
    Animate
  </button>
</div>

<div class="mt-5">
  <h5>templates/timeseries.hbs</h5>
  <CodeSnippet @name="timeseries.hbs" />
</div>

<div class="mt-5">
  <h5>controllers/timeseries.js</h5>
  <CodeSnippet @name="timeseries.js" />
</div>
